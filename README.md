# mo824-final_project
Implements 5 distinct Genetic Algorithms to optimize Neural Network hyperparameters. This project tunes a model on the MNIST dataset to find the most effective configuration and compares the performance of each GA

The instances (MNIST dataset) will be loaded directly by tensorflow library when creating the neural network